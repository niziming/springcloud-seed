# application.yml

server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/seata_order?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: # 请在这里填入你的数据库密码
    driver-class-name: com.mysql.cj.jdbc.Driver

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

# mybatis等其他业务配置...