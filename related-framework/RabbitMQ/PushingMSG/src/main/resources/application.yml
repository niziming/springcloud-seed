spring:
  application:
    name: pushing
  main:
    allow-bean-definition-overriding: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    exchange: zm.exchange
    queue: zm.queue
    routingkey: zm.routingkey
server:
  port: 8001