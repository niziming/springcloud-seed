spring:
  application:
    name: admin-client
  boot:
    admin:
      client:
        url: http://localhost:8712
server:
  port: 8713

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS