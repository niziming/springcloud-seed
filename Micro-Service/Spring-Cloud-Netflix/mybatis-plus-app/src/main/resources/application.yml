server:
  port: 8701
spring:
  profiles:
    active: common, plus
  application:
    name: netflix-mybatisplus-app-8701

logging:
  level:
    cn.zm: debug

# eureka
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8700/eureka/
