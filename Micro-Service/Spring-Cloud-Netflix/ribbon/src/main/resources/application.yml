server:
  port: 8703
spring:
  profiles:
    active: common
  application:
    name: ribbon-8704

logging:
  level:
    cn.zm: debug

# eureka
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8700/eureka/
